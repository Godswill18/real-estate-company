

(function (factory) {
    if (typeof define === 'function' && define.amd) {
      // If using AMD (Asynchronous Module Definition), define the module with jQuery as a dependency.
      define(['jquery'], function (jQuery) {
        return factory(jQuery);
      });
    } else if (typeof module === 'object' && typeof module.exports === 'object') {
      // If using CommonJS (Node.js) or similar environments, export the result of factory(jQuery).
      module.exports = factory(require('jquery'));
    } else {
      // For browser environments, execute factory(jQuery).
      factory(jQuery);
    }
  })(function ($) {
    // Define custom easing functions here
    function easeInQuad(n) {
      return n * n;
    }
  
    function easeOutQuad(n) {
      return 1 - (1 - n) * (1 - n);
    }
  
    // Add more easing functions as needed
  
    // Register custom easing functions with jQuery
    $.extend($.easing, {
      def: 'easeOutQuad', // Default easing function
      swing: function (p) {
        return $.easing[$.easing.def](p);
      },
      easeInQuad: easeInQuad,
      easeOutQuad: easeOutQuad,
      // Add more easing functions as needed
    });
  });
  